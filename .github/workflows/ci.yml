name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # The connector's whole point is being cross-platform, and the psutil
        # differences it works around (no sensors_temperatures off Linux,
        # placeholder cpu_freq on macOS, drive letters on Windows) only show up
        # if all three actually run the suite.
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      # A project venv from uv.lock, not --system: the runners' Linux and
      # macOS Pythons are externally managed (PEP 668) and refuse the install.
      - name: Install
        run: uv sync --locked --extra dev --python 3.13
      - name: Lint
        if: matrix.os == 'ubuntu-latest'
        run: |
          uv run --locked black --check bin keelson_connector_pc tests
          uv run --locked pylint bin keelson_connector_pc
      - name: Unit tests
        run: uv run --locked pytest -vv -m "not e2e"
      - name: End-to-end tests
        run: uv run --locked pytest -vv -m e2e

  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build image
        run: docker build -t keelson-connector-pc .
      - name: Smoke test the entry point
        run: docker run --rm keelson-connector-pc "pc2keelson --help"
